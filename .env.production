# .env.production
VITE_CMSSDT_SERVER=
##VITE_USE_PROXY=false
VITE_CMSSDT_API_BASE=http://194.12.165.1:8002
# See .env.development - same shift-console proxy backend. Only used if this app is
# served via `vite preview`; a static Nginx/CDN deployment must mirror the six
# /shift* and /api/shift-*, /api/alerts/status rewrite rules from vite.config.js itself.
VITE_SHIFT_API_BASE=http://194.12.165.1:8002